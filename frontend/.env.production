# Build de produção (Vercel) — demo com dados mockados.
# Quando o backend estiver hospedado, troque VITE_USE_MOCK para false
# e defina VITE_API_BASE_URL com a URL pública do backend.
VITE_USE_MOCK=true
